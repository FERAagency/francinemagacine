# Secrets — never commit
.env
.env.*
*.pem
*.key

# Local Claude Code settings (may contain machine-specific paths)
.claude/settings.local.json

# Build output — regenerated by .claude/rebuild-zip.py
deploy.zip

# Unrelated working doc (Showcase Lab brief, contains personal details)
images/showcase-lab-about-master-brief.odt
